// Header is now fully integrated into the HomeScreen structural flow.
// This empty file prevents imports from crashing if other files still reference it,
// but for the ultimate modern feel, we don't use a floating separate header in the Home dashboard anymore.
export default function Header() {
  return null;
}
